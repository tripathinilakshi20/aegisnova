# AEGISNOVA

AI + Edge Intelligence + IoT + Digital Twin prototype.

Execution: hardware → pretrained YOLO → real dataset if needed → training/evaluation → Pi deployment → backend → dashboard → integration → Digital Twin.
